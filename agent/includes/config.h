// This is a dummy config header. Dont use this in production
// Instead, generate a client through the server's CLI options

#define GLOB_ID "TEST_AGENT" // These two values should at some 
#define GLOB_LOGIN "lala"
#define HOST "127.0.0.1"
#define PORT "22"
